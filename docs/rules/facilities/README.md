# Facilities

Der Facilities-Katalog ist die editierbare Referenz für **Einrichtungen** (Keys, Kosten, Effekte, Caps).

Startpunkt: `docs/rules/facilities/catalog.md`.

Soll (detaillierte Einrichtungen & Spezialisierungen): `docs/rules/soll/facilities.md`.

Konvention:
- Jede Facility hat einen **stabilen Key** (`facilityKey`), der im Code referenziert wird.
- Effekte sind strukturiert nach: Ertrag/Unterhalt, DC/Caps, Markt/Conversion, Bonusaktionen, Prereqs/Caps.

Backlog/Spezifikation (vNext): TBD (wird bei Bedarf aus `docs/rules/soll/facilities.md` abgeleitet).
